@page
@model DbAddMigration.Pages.myScaffold.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Bil[0].Navn)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Bil[0].Efternavn)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Bil[0].Alder)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Bil[0].By)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Bil[0].IsDeleted)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Bil)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Navn)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Efternavn)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Alder)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.By)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IsDeleted)
                </td>

                <td>
                    <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
